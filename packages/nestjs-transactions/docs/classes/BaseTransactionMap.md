# Class: BaseTransactionMap

Give transaction map a set of indexes to not make it go crazy when some events are missing from it.
They are optional after all.

## Indexable

▪ [k: `string`]: `any`

## Constructors

### constructor

• **new BaseTransactionMap**()
