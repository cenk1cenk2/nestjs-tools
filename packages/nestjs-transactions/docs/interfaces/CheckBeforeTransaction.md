# Interface: CheckBeforeTransaction

The type of parallel transactions which will be run after.

## Properties

### check

• **check**: [`Transaction`](../types/Transaction.md)<`never`, `void`\>

#### Defined in

transactions-manager.interface.ts:20
